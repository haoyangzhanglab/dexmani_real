# DexMani Real Post-refactor Integration Cleanup Progress

This is the durable checkpoint for the post-namespace-refactor integration and
cleanup workflow. The completed namespace-refactor checkpoint remains read-only.

## Workflow state

```yaml
baseline_head: f3e3cc064cb5537d605a06c383263ec776164c6c
baseline_kind: current_uncommitted_post_refactor_worktree
status: complete
auto_advance: true
compact_after_each_accepted_phase: true
accepted_phases: [0, 1, 2, 3, 4, 5]
current_phase: null
next_phase: null
write_owner: null
```

## Preserved starting state

- The entire current worktree is the baseline. It contains the accepted Phase 0–9
  namespace/ownership refactor, agent workflow changes, the user-provided v5 brief,
  and its completed progress checkpoint.
- Old files shown as deleted and target files shown as untracked are paired refactor
  moves until staged; no agent may restore an old path or omit a target file.
- No commit is authorized. Preserve all existing changes and stop at a validated
  worktree unless the user later requests staging or commits.

## Phase sequence

```text
0 baseline and consumer inventory
1 imports, exports and dynamic targets
2 persistence, readers and resource paths
3 examples entry-point acceptance
4 obsolete, compatibility, temporary and redundant code removal
5 documentation synchronization
Final DoD
```

## Current phase capsule

- Scope: workflow complete; no next phase or write owner.
- All code, examples, cleanup and documentation phases passed their focused review and
  the final offline gate.
- The complete result remains in the uncommitted worktree. Old paths shown as deleted
  and target paths shown as untracked must be staged together if a later commit is
  explicitly authorized.

## Last accepted phase

Final DoD — accepted.

- Phase 0 locked the accepted namespace-refactor worktree and mapped every consumer,
  example side effect, process target and persistence boundary.
- Phase 1 resolved all imports, exports, patch strings and dynamic targets without needing
  compatibility code.
- Phase 2 made processed raw-source provenance canonical and cwd-independent while
  preserving strict replay hash validation and every persistence/schema invariant.
- Phase 3 validated all 13 examples at their safe side-effect level and corrected current
  schema and side-effect descriptions.
- Phase 4 removed one reader compatibility alias, the unsupported asset-path fallback,
  stale ownership text, redundant package imports and obsolete generated bytecode.
- Phase 5 synchronized current user and focused implementation documentation.
- No optional example rename, staging or commit was performed.

## Invariants

- Preserve safety, IPC, schema, configuration, timing, limits, collision and hardware
  ownership semantics.
- All Python commands use `conda run -n real_robot`.
- Do not run hardware, RealSense, homing, physical replay, calibration writes,
  `execute=True`, CUDA runtime or a real Policy checkpoint.
- The external `/home/zhanghaoyang/Desktop/dexmani_policy` repository is read-only.
- Do not add compatibility aliases, shims, fallback branches, retries, temporary code
  or new abstraction layers to complete cleanup.
- One package/import graph has one write owner. Reviews and inventories remain read-only.

## Acceptance contract

Each phase performs a focused check, residue audit and parent acceptance before this
checkpoint is updated and context is compacted. The parent runs the full offline gate
only after coherent code changes, and does not repeat a passing gate without new code.

## Validation evidence

- Inherited namespace-refactor Final DoD: `198 passed, 106 subtests passed`, compileall
  passed, `git diff --check` passed, exact old-path scan clean, and sol-high review READY.
- Phase 0 `conda run -n real_robot python -m compileall -q dexmani_real examples tests`:
  passed.
- Phase 0 exact active old-import scan: clean.
- Phase 1 focused tests: 53 passed, 24 subtests passed.
- Phase 1 import audit: 640 absolute imports, 22 relative imports and 183 patch targets;
  all resolved.
- Phase 1 package discovery/import-isolation audit: passed for 23 packages.
- Phase 2 focused tests: 26 passed, 24 subtests passed.
- Phase 2 full gate: 199 passed, 106 subtests passed; compileall and diff check passed.
- Default canonical config SHA and cameras.json hash/size remain unchanged; schema
  identities remain raw v24, processed v12 and Policy Zarr v6.
- Phase 3 focused tests: 21 passed, 24 subtests passed.
- All safe offline `--help` checks, examples compileall, focused Black check and examples
  diff check passed. Hardware-affecting entries were static-only.
- Phase 4 focused tests: 28 passed.
- Phase 4 full gate: 199 passed, 106 subtests passed; compileall and diff check passed.
- Canonical asset root and all four robot URDF/SRDF resources resolve and exist.
- Phase 5 focused document/path/schema wording scans and `git diff --check`: passed.
- Phase 5 local Markdown link audit: 123 checked, 0 missing.
- Final `conda run -n real_robot python -m compileall -q dexmani_real examples tests`:
  passed.
- Final `conda run -n real_robot python -m pytest -q`: 199 passed, 106 subtests passed.
- Final exact old namespace, old alias and stale v10 scan: clean.
- Final package-root import smoke loaded no Torch, RealSense, Pinocchio, MPlib, xArm or
  DexMani Policy backend.
- Final canonical config SHA remains
  `df2f30a229242f86909a0350bad620f2084a898a81a260c4b610cba65683b0cd`;
  raw/processed/Zarr schemas remain 24/12/6.
- Final `cameras.json` remains 282 bytes with SHA-256
  `5e33e93f12aff758e4d2d3cb6137610b7e40e2f3c00324ee56832df6c49e26f5`.
- Final tree contains exactly the eight accepted second-level namespace boundaries and no
  temporary/backup artifacts. `git diff --check` passed.
- No hardware, RealSense, live VR, homing, physical replay, calibration write, real Policy
  restore or CUDA runtime was exercised by this workflow; no such validation is claimed.

## Findings and blockers

None. All phase owners and required luna-max/sol-high reviews returned READY. One failed
resource smoke command used a nonexistent test constant name; the corrected command used
the source-owned four robot resource constants and passed. This was not a product defect.

## Next phase entry

None. The post-refactor integration and cleanup workflow is complete in the worktree.
No commit was authorized or created.
