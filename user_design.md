# User Design Decisions

本文记录项目使用者确认的运行行为取舍及其实现边界；当前实际行为仍以源码和解析后的
运行时配置为准。

实现状态：VR 与键盘遥操作均已采用这些取舍；物理回放、homing 与其他 table-aware
路径不随之改变。

## 1. 遥操作不使用桌面碰撞作为动作拒绝条件

在遥操作过程中，不检测机械臂或灵巧手与桌面的碰撞，不因预测到桌面接触而拒绝 IK
候选或保持上一条动作。桌面附近的精细抓取需要机器人有意接近甚至接触桌面，而桌面
标定误差、碰撞网格余量和离散检测容易把可执行的抓取动作误判为碰撞。

此选择只针对遥操作中的“机器人—桌面”碰撞门控，不关闭以下保护：

- 机器人自碰撞检测，包括机械臂、机械臂与灵巧手之间的碰撞；
- 关节限位、工作空间、命令新鲜度、运行代际和生命周期检查；
- 设备 SDK 自身的安全机制、急停和操作者暂停能力；
- homing、物理回放及离线诊断各自需要的路径或碰撞验证。

这是为桌面精细操作选择的可接受风险取舍，不表示机械臂或灵巧手与桌面发生碰撞在物理上
没有风险。遥操作仍应使用适合精细操作的速度，并由操作者保持对真实场景的直接监督。

## 2. XHand 抓取过流按可恢复接触事件处理

XHand 采用位置控制抓取物体时，手指接触物体后无法继续到达目标位置，电流上升或出现
短暂过流是预期的接触现象。单独的、可恢复的过流不应中止抓取、触发全局错误或自动丢弃
整段采集数据；在 XHand 仍能执行命令且后续能够取得新鲜有效状态时，系统正常继续运行，
同时累计过流计数。

判断依据是 SDK 操作和数据通路是否仍然有效，而不是 Python 日志显示为 `WARNING` 还是
`ERROR`。当前已知过流码 `1501035` 虽通过 `XHandError` 返回，但设计上属于可恢复过流；
该次失败读取不发布缓存值构造的反馈帧，下一次成功读取携带累计过流计数。若始终不能恢复，
现有反馈新鲜度检查负责暂停动作并升级。

以下情况不属于可忽略的正常抓取过流，应沿现有安全边界升级处理：

- SDK 错误导致命令无法继续执行，或无法恢复新鲜、有效的状态读取；
- 通信错误、板卡错误、设备断连或其他硬件故障；
- 过流伴随持续的数据无效，使当前采集不再满足数据合同；
- 系统进入既有 watchdog、全局错误或急停条件。

因此，本设计允许“抓取接触导致的可恢复过流”继续运行，但不把影响控制能力或数据真实性
的 SDK/硬件错误降级为正常事件。
