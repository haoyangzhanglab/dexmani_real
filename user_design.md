# User Design Decisions

本文记录项目使用者确认的运行行为取舍及其实现边界；当前实际行为仍以源码和解析后的
运行时配置为准。

实现状态：VR 与键盘遥操作、物理回放的轨迹回放环节均已采用这些取舍；homing
（含回放 return_home）与其他 table-aware 路径不随之改变。

## 1. 遥操作不使用桌面碰撞作为动作拒绝条件

在遥操作过程中，不检测机械臂或灵巧手与桌面的碰撞，不因预测到桌面接触而拒绝 IK
候选或保持上一条动作。桌面附近的精细抓取需要机器人有意接近甚至接触桌面，而桌面
标定误差、碰撞网格余量和离散检测容易把可执行的抓取动作误判为碰撞。

此选择只针对遥操作中的“机器人—桌面”碰撞门控，不关闭以下保护：

- 机器人自碰撞检测，包括机械臂、机械臂与灵巧手之间的碰撞；
- 关节限位、工作空间、命令新鲜度、运行代际和生命周期检查；
- 设备 SDK 自身的安全机制、急停和操作者暂停能力；
- homing 及离线诊断各自需要的路径或碰撞验证；物理回放的碰撞验证见 §3。

这是为桌面精细操作选择的可接受风险取舍，不表示机械臂或灵巧手与桌面发生碰撞在物理上
没有风险。遥操作仍应使用适合精细操作的速度，并由操作者保持对真实场景的直接监督。

## 2. XHand 抓取过流按可恢复接触事件处理

XHand 采用位置控制抓取物体时，手指接触物体后无法继续到达目标位置，电流上升或出现
过流是预期的接触现象。已确认的 SDK 过流码 `1501035` 在发送位置命令时视为“命令已接受
但发生抓取接触”：不抛出 `XHandError`、不中止抓取、不触发全局错误或自动丢弃整段采集
数据，并照常更新最后下发的目标位置。hand worker 同时累计该事件，供 episode 质量指标
记录，而不将其作为命令通路或设备健康失败。

这是对发送响应的明确选择，不会放宽状态读取合同：`get_state()` 若返回同一码，仍不发布
用缓存值伪造的新鲜反馈；下一次成功读取会携带累计计数。判断依据是 SDK 操作和数据通路
是否仍然有效，而不是 Python 日志显示为 `WARNING` 还是 `ERROR`。若状态读取始终不能恢复，
现有反馈新鲜度检查负责暂停动作并升级。

以下情况不属于可忽略的正常抓取过流，应沿现有安全边界升级处理：

- SDK 错误导致命令无法继续执行，或无法恢复新鲜、有效的状态读取；
- 通信错误、板卡错误、设备断连或其他硬件故障；
- 过流伴随持续的数据无效，使当前采集不再满足数据合同；
- 系统进入既有 watchdog、全局错误或急停条件。

因此，本设计允许“抓取接触导致的可恢复过流”继续运行，但不把影响控制能力或数据真实性
的 SDK/硬件错误降级为正常事件。

## 3. 物理回放不使用桌面碰撞作为拒绝条件（return_home 除外）

物理回放重放的是遥操作录制的精细抓取轨迹，而遥操作本身不以桌面碰撞作为动作拒绝条件
（见 §1）：录制轨迹可能有意贴近甚至接触桌面。若回放前检查再以标定桌面作为拒绝条件，
桌面标定误差、碰撞网格余量和离散检测同样会把可执行的抓取轨迹误判为碰撞，使遥操作录制
的抓取轨迹系统性地无法回放。因此，物理回放的 preflight 与轨迹回放执行过程不把
机器人—桌面碰撞作为拒绝或中止条件。

此选择只针对物理回放轨迹回放中的“机器人—桌面”碰撞门控，不关闭以下保护：

- 机器人自碰撞检测与静态障碍物盒碰撞检测；
- preflight 的工作空间逐段校验、provenance 一致性、首帧实测状态到首命令的过渡校验与
  手部数据认证，以及运行时动作安全门的关节限位与工作空间校验；
- return_home（homing）路径的桌面间隙校验，包括 soft clearance；
- 设备 SDK 自身的安全机制、急停和操作者暂停能力。

这一取舍与 §1 一致，不表示机械臂或灵巧手与桌面发生碰撞在物理上没有风险。回放期间
操作者仍应保持对真实场景的直接监督。

## 4. XHand 连接后无条件发送 home reset

每次 XHand worker 成功连接并完成触觉初始化后，在发布 ready 前向设备发送一次配置的
`home_qpos`。该动作是设备初始化 reset，不执行桌面或碰撞规划，也不等待物理位置收敛；
SDK 未接受命令时 worker 启动失败。操作者连接设备前必须确保手指周围无障碍物。

物理回放以 episode 首帧的**实测 arm `qpos`** 作为物理起点，而不是将首帧 arm 命令
误作实测姿态。xArm 必须在 replay 前由操作者通过独立受监督流程移动到该起点；若偏差超过
5°，replay 在发送额外命令前拒绝。XHand 因连接 reset 无法在连接前保持录制姿态：replay
在同一已连接会话内保持 xArm 当前姿态，先检查实时 arm/hand 状态到 frame 0 手部命令的
自碰撞/静态障碍过渡，再发送该命令并维持 3 秒 warm-up。该 warm-up 不写入 replay 指标，
随后仍从 frame 0 重放原始命令流。

手部命令目标与实测状态允许不同：接触、编码零偏和稳态误差可能令实测值不能复现录制时的
反馈值。因此不将实时手部 `qpos` 与录制首帧 `qpos` 的偏差作为起点通过条件；首帧实测
手部状态仅用于离线 preflight 的录制状态到首命令碰撞校验。运行时继续要求新鲜有效的手部
反馈、首帧命令的操作/机械限位校验，以及实时起点过渡的碰撞检查；这些任一项失败仍
fail-closed。
