# XHand RS485 通信与触觉温度诊断记录

## 状态

**未关闭：在完成硬件链路排查并获得足够的无错误运行记录前，不应将此 XHand
RS485 链路用于无人看守控制、需要可靠位置确认的任务，或依赖触觉温度的保护逻辑。**

本文记录用户提供的 `examples/xhand_control_example.py` 与键盘遥操作日志的离线
分析结果。日志未包含实际日期；下述结论仅覆盖已观察到的运行，不替代硬件厂商诊断。

## 设备与运行条件

| 项目 | 记录值 |
|---|---|
| 通信方式 | RS485，经 `/dev/ttyUSB0` |
| 波特率 | 3,000,000 bit/s |
| XHand software SDK | 1.4.2 |
| XHand hardware SDK | 1.1.0 |
| 手部类型 | R，`device_id=0` |
| 序列号 | `012R320220251128022` |
| 打开后等待 | 1.0 s |

## 已观察到的现象

### 1. RS485 CRC 错误：`1501070`

`1501070` 的 SDK 信息为 `Communication data CRC error`。它表示本次通信事务的
数据校验失败，主机不能把该次响应当作可信确认。

在三次连续的独立诊断运行中：

| 运行 | 位置 | 结果 |
|---|---|---|
| 1 | `palm`、`ok` | 首次发送 CRC 错误；等待 80 ms 后各重试一次并成功。 |
| 2 | `palm` | 首次发送及唯一一次重试均为 CRC 错误；示例停止后续动作并关闭设备。 |
| 3 | 最终 `home` | 首次发送 CRC 错误；80 ms 后重试成功。 |

此前的键盘遥操作启动日志还出现过一次 RS485 **状态读取** CRC 错误，并在重试后恢复。
因此该问题同时覆盖读、写两类事务，且跨独立运行重复出现；不能作为一次性启动抖动处理。

### 2. 触觉温度暂时不可用：`1501020`

`1501020` 的 SDK 信息为 `Sensor fails to read temperature`。当前驱动将它视为
触觉数据的部分降级：温度字段无效，但合力和 120 点原始触觉力仍被标记为有效。

已观察到：

- 一次 `palm` 指令返回 `1501020`；连续只读刷新后仍暂时不可用。
- 另一次 `palm` 指令返回 `1501020`；80 ms 后只读刷新恢复为完整帧，finger 5 温度为
  35。
- 启动时的状态读取正常：`comm_err=0`、`joint_err=0`、`tip_err=0`，原始触觉力有
  120 个有限值点，温度为 34 或 35。

这表明触觉温度通道是间歇性失效，不能凭现有记录判定为永久损坏。

## 诊断结论

### 主问题：RS485 链路可靠性不足

多个命令的首次传输可由重试恢复，但至少一次发送在重试后仍失败。对真实机器人系统，
这已构成通信可靠性问题，而不是可忽略的日志噪声。

对于最终 CRC 失败的命令，**不能推断动作一定未执行**：设备可能已收到命令而响应帧
损坏，也可能未收到完整命令。关闭并重连后必须读取实时关节状态重新建立基准；不得根据
软件预期姿态继续发送动作。

### 次问题：触觉温度遥测不稳定

`1501020` 不表示关节电机或整条总线失效。动作被 SDK 标记为已发送，且温度以外的触觉
字段可用；但任何使用温度的监测、补偿或保护必须将该帧视为温度无效。

### 根因假设与证据边界

以下为按优先级排序的假设，尚未被本日志单独证实：

1. RS485 物理链路抗干扰裕量不足：线缆、接头、USB-RS485 转接器、终端匹配、A/B
   线路、屏蔽、公共参考地或布线均可能导致 CRC 错误。
2. 动作期间的电源瞬态或电磁干扰：错误多出现在动作指令附近，电机电流变化可能造成
   电压跌落、地弹或串扰；这也可能使触觉温度帧暂时不完整。
3. 手部内部触觉板、线束或固件的时序问题：`1501020` 在没有 CRC 报错的事务中也会
   出现，故不能只归因于主机侧 RS485。
4. SDK/固件兼容性需向厂商确认，但仅凭 software SDK 1.4.2 与 hardware SDK 1.1.0
   不能认定存在版本不兼容。

## 排查与恢复顺序

1. 在发生最终 `1501070` 后，保持停止状态，关闭设备；重新打开后先读取实时关节状态，
   确认实际姿态，再决定是否继续。
2. 检查并必要时替换手部线束、连接器和 USB-RS485 转换器；确认 A/B 极性、双绞屏蔽、
   屏蔽接地、可靠公共地，以及仅在总线两端使用符合硬件要求的终端电阻。
3. 将 RS485 信号线与电机电源线、机械臂驱动线分离；在手部动作期间测量设备端供电与
   地参考，排查压降和噪声。
4. 在静止与各预设动作后分别重复采样，记录 `1501070`、`1501020` 的次数、对应动作、
   关节姿态、供电测量和环境布线，以判断是否与运动相关。
5. 仅在厂商确认支持时，比较另一只合格转接器或较低波特率下的错误率；不要自行改变
   未经确认的总线参数或更新固件。
6. 外部链路改善后仍复现时，向厂商提供本记录、错误码统计和复现步骤，要求检查手部
   内部通信、触觉板与固件兼容性。

## 通过标准

在相同线缆、供电和动作条件下，重复诊断应满足：

- 所有状态读取与命令发送均返回 `error_code=0`，无 `1501070`。
- 多轮预设动作及回零后，触觉温度持续可读，无 `1501020`。
- 关节、通信和指尖板错误持续为零。
- 若发生任何通信错误，记录现场条件并停止将该次执行结果作为可重复实验数据。
